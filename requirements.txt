torch>=2.0.0
torchvision>=0.15.0

diffusers>=0.36.0
transformers>=4.41.2
accelerate>=0.31.0
huggingface_hub>=0.23.0
peft>=0.17.0
safetensors>=0.4.0
datasets>=2.20.0
tensorboard>=2.13.0
PyYAML>=6.0
Jinja2>=3.1.0

# Tokenizers (needed for some Transformer text encoders, e.g. T5 in SD3.5)
sentencepiece>=0.1.99
protobuf>=3.20.0

# Utilities used by the notebooks / repo helpers
ftfy>=6.0.0
matplotlib>=3.3.0
numpy>=1.23.0
Pillow>=8.0.0
packaging>=20.0
einops>=0.6.0

# Optional (only needed for CLI usage in src/fine_data_generation/run.py)
# pyrallis>=0.3.0

# Optional (only needed for cross-attention visualization helpers)
# opencv-python>=4.5.0
